version: 2
jobs:
  build:
    docker:
      - image: buildpack-deps:trusty
      - image: circleci/node:latest
    working_directory: ~/vscode-notification-tester
    steps:
      - checkout
      - run: yarn install
      - run: yarn test
