version: 2
jobs:
  build:
    machine:
      image: circleci/classic:latest
    working_directory: ~/vscode-notification-tester
    steps:
      - checkout
      - run: yarn install
      - run: yarn test
