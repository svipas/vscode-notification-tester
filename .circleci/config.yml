version: 2
jobs:
  build:
    docker:
      - image: circleci/node:latest
      - image: circleci/node:carbon
    working_directory: ~/vscode-notification-tester
    steps:
      - checkout
      - run: yarn install
      - run: yarn test
